name: github ci/cd pipeline project

on:
  push:
    branches: [ "master" ]

env:
  MAJOR: '1'
  MINOR: '0'
  PATCH: ${{ github.run_number }}

jobs:
  build:
    runs-on: ubuntu-latest
         
    steps:
    - uses: actions/checkout@v3
    
    - name: check files
      run: ls -la

    - name: Build the Docker image
      run: docker build --build-arg PATCH_NUM=$PATCH --build-arg MAJOR_NUM=$MAJOR --build-arg MINOR_NUM=$MINOR -t javaproject:latest .
      
    - name: show_image
      run: docker images
      
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

        
    - name: docker tag
      run: docker tag javaproject:latest avkh12345/github:latest

    - name: push latest
      run: docker push avkh12345/github:latest

  #  - name: tag version
  #    run: docker tag avkh12345/github:latest avkh12345/github:$MAJOR_NUM.$MINOR_NUM.$PATCH_NUM 
      
 #   - name: push latest
#      run: docker push avkh12345/github:latest
      
#    - name: push version
#      run: docker push avkh12345/github:$MAJOR_NUM.$MINOR_NUM.$PATCH_NUM
